# URL of your deployed Backend service (e.g. Render / Railway / Fly.io)
# For local development, this defaults to http://localhost:5000 if left empty.
VITE_BACKEND_URL=https://your-backend-service.onrender.com
